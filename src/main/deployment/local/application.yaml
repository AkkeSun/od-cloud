spring:
  application:
    name: od-cloud
  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    generate-ddl: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: akkessun@gmail.com
    password: ffotcgwhdsmoodmo
    protocol: smtp
    default-encoding: utf-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connection:
            timeout: 5000
            write-timeout: 5000
          ssl:
            enable: false

  datasource:
    url: jdbc:h2:mem:accounts-test;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

service-constant:
  jwt:
    ttl: 3600000 # 1 hour
    secretKey: ENC(csG5ltVnv01zZ1pql649hKCQ6hiCeHdh)